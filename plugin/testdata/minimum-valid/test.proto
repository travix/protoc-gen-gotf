syntax = "proto3";

package goterraform;

option go_package = ".;testpb";

import "plugin.proto";

option (provider) = {
  name: "valid"
};

message User {
  option (resource) = {
  };
  string email = 1;
}

message Group {
  option (datasource) = {
  };
  repeated User members = 1;
}
